# 项目级 Agent 工程约束

本文件适用于本仓库中的所有代码、测试和文档变更。

## 单一业务路径

- 修改现有功能时，直接更新当前权威实现，最终只保留一个现行业务路径。
- 不得以兼容、备用或渐进迁移为由，长期并行保留旧路由、旧服务、旧状态机、旧入口、旧配置或重复业务逻辑，以免误导使用者和后续维护者。
- 同一次变更应同步删除废弃实现及其引用、测试、配置和文档，不得保留静默 fallback、双写或无法确认实际命中路径的分支。
- 必须迁移旧数据时，迁移逻辑应与日常业务路径隔离；迁移完成后移除临时代码，业务运行时仍只能有一个权威路径。
- 测试和文档只描述、验证当前权威路径，不得继续把已废弃路径呈现为可用方案。

## Agent 语义决策边界

- 本项目是 Agent 系统。自然语言的语义理解、用户意图判断、业务目标选择、工具选择和执行规划应由当前模型结合用户最新消息、真实业务数据与可用工具完成，应用代码不得另建一套简化的语义决策器。
- 禁止使用正则表达式、关键词包含、固定短语表、启发式评分或硬编码分支，从用户自然语言中推断业务意图、选择业务流程、绑定目标实体或替 Agent 决定下一步工具。不同语言、数字写法、别名和自由表达必须进入同一模型决策路径。
- 发现“第2章／第二章／下一章”等表达变体时，正确修复不是继续扩充正则或同义词表，而是删除这套语义匹配器，让 Agent 读取真实实体并返回结构化 ID；应用层只验证该 ID 是否存在、归属正确且类型可用。
- 正则和确定性解析只可用于非语义工作，例如结构化字段格式校验、协议解析、数据清洗、检索分词和模型已经选定目标后的规范化；不得把这些结果提升为用户意图或业务目标。
- 工具契约已经定义枚举或独立字段时，模型必须直接返回对应结构，例如把 `role_type` 写成枚举、把人物身份写入 `background`。应用层不得再用别名表、分词或正则把“主角，穿越者”等自然语言拆分补写到多个字段；无效结构应由同一模型按契约修正，或明确失败。
- 应用代码负责模型不能替代的确定性边界：工具权限、项目与实体归属、实体类型、参数结构、事务、幂等、并发锁、状态机、回合终止和错误持久化。需要强制保证的行为应在工具执行或状态转换处校验，而不是在模型调用前猜测用户说法。
- API、本机 CLI、MCP 和手机端应向 Agent 提供各自已授权且与实际执行一致的工具契约；提示词负责说明工作流，工具结果和状态机负责兑现边界，不得因入口不同重新实现自然语言路由。
- 全量业务工具必须在同一权威目录中按宽粒度能力类别归类。每个模型回合最初只开放 `set_tool_categories`；模型用它替换下一模型步骤的开放类别，类别切换后当前步骤立即结束。运行时只做类别、授权与真实工具注册表的交集，不得根据用户文本替模型预开工具。

## 手机端独立运行与 PC 端一致性

- PC 端和手机端具有相同功能时，手机端必须能够在产品定义的移动端架构内独立完成完整流程，不得依赖 PC 客户端正在运行、PC 界面状态或仅存在于 PC 端的隐式上下文。
- 手机端与 PC 端必须遵循同一业务契约，包括输入校验、权限边界、状态流转、数据读写、取消与重试、错误处理以及最终结果语义。
- 可以复用共享协议、数据模型和后端能力，但不得把手机端实现成依赖 PC 界面代办的转发壳，也不得使用功能缩水的替代流程冒充同一功能。
- 新增或修改两端共有功能时，必须同时检查两端实现和测试；验收应覆盖手机端独立运行，以及相同输入在两端产生一致的关键状态和业务结果。
- 确需存在平台差异时，必须明确记录差异、原因和影响，不能以未说明的分支或旧路径形成隐性不一致。

## 用户意图与界面上下文

- 用户最新一条明确消息是当前任务的唯一意图来源；历史对话、编辑器选中项、当前页面和上次打开对象只能作为参考上下文，不能覆盖或暗中改写本轮目标。
- 章节序号、中文数字、标题别名、“下一章”等自然语言目标必须遵循上述 Agent 语义决策边界，由当前 Agent 结合真实项目数据理解并取得真实 ID；不得把界面当前选中节点直接传给生成器充当任务目标。
- 模型选定目标后，写入工具必须校验 ID 属于当前作品且实体类型正确；章级工具不得接受卷、分区或场景节点。API、本机 CLI、MCP 与手机端必须遵循同一目标解析语义。

## 未保存草稿与异步界面状态

- AI 生成的新章节先形成独立的未保存草稿，不能更新、覆盖或伪装成任何已保存章节；只有作者明确保存后才能写入正式章节。
- 未保存草稿存在时，正文编辑器以该草稿为唯一显示源；此前选中章节的迟到请求必须取消或忽略，不能回写覆盖草稿。界面应将未保存草稿与正式章节分开展示。
- 章节草稿生成成功即结束模型回合。保存、是否启动建档以及何时继续下一章均由作者决定；建档完成前不得让 AI 自动续写下一章。
