# 司命 3.3.2

3.3.2 修复启用 Gateway 后，历史角色 AI 配置同步任务可能导致桌面端启动超时的问题。

## 修复

- 将 `character_ai_config` 补入桌面端、Gateway 与 Android 共用的同步实体契约。
- 增加捕获实体与公开同步协议的一致性回归测试，防止后续新增实体时再次遗漏。
- Gateway 同步队列恢复改为非阻塞；单条过期或异常任务会记录日志并保留重试，不再阻止整个桌面应用启动。
